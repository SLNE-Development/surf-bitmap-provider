name: Publish to Maven & Create GitHub Release

on:
  push:
    branches:
      - version/**
  workflow_dispatch:

jobs:
  build:
    uses: SLNE-Development/surf-workflows/.github/workflows/build-publish-release-gradle.yml@master
    with:
      modules: "surf-bitmap-provider-velocity-*-all.jar;surf-bitmap-provider-placeholders-*-all.jar;surf-bitmap-provider-paper-*-all.jar"
    secrets: inherit
