package dev.slne.surf.bitmap.bitmaps

import dev.slne.surf.bitmap.api.BitmapProvider
import dev.slne.surf.bitmap.bitmaps.bitmaps.LettersRed

enum class Bitmaps(
    val provider: BitmapProvider
) {
    LETTERS_RED(LettersRed),
    ;
}